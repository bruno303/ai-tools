# Benchmark fixtures are intentionally incomplete or incorrect by design so agents have
# something to fix. Hidden tests are evaluator artifacts rather than production code.
sonar.exclusions=benchmarks/scenarios/**/fixture/**,benchmarks/scenarios/**/hidden-tests/**
